<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="change.village">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="change.village.Village"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Village">

        <activity
            android:name="change.village.activity.login.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="change.village.activity.login.RegisterActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name="change.village.activity.login.PhoneNumberVerifyActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name="change.village.activity.main.MainActivity"  />

        <activity
            android:name="change.village.activity.main.home.ItemAddActivity"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name="change.village.activity.main.home.DetailView" />

        <activity
            android:name="change.village.activity.chat.ChatActivity"
            android:windowSoftInputMode="adjustResize"/>

    </application>

</manifest>